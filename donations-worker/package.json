{
  "name": "donations-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "cross-env NODE_ENV=production nodemon index -L",
    "start:dev": "cross-env NODE_ENV=development nodemon index -L"
  },
  "dependencies": {
    "babel-register": "^6.26.0",
    "dotenv": "^6.0.0",
    "firebase": "^5.3.1",
    "moment": "^2.18.1",
    "nodemon": "^1.11.0",
    "request": "^2.81.0",
    "request-promise-native": "^1.0.4",
    "winston": "^2.3.1",
    "winston-loggly-bulk": "^2.0.3"
  },
  "devDependencies": {
    "babel-preset-env": "^1.7.0",
    "cross-env": "^5.0.1"
  }
}
